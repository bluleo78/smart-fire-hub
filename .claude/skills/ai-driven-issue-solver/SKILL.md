---
name: ai-driven-issue-solver
description: docs/ISSUES.md에 등록된 버그/개선 이슈를 자동으로 분석하고 코드 수정 → 테스트 → ISSUES.md 상태 업데이트 → 커밋까지 one-shot으로 처리한다. "55번 이슈 처리해줘", "첫 번째 미처리 이슈 고쳐줘", "Major 이슈 수정해줘", "#54 해결해줘" 같은 요청에 반드시 이 스킬을 사용한다. 이슈 번호나 이슈 수정/해결/처리 의도가 조금이라도 보이면 반드시 발동한다.
---

# AI-Driven Issue Solver

docs/ISSUES.md 이슈를 one-shot으로 처리한다.

---

## Step 1. 타겟 이슈 선택

**사용자가 번호를 지정한 경우**: 해당 번호의 이슈를 선택한다.

**지정하지 않은 경우**: `docs/ISSUES.md`에서 🔴 미처리 이슈 중 심각도 순(Critical > Major > Minor > UX)으로 첫 번째를 선택한다.

선택 후 사용자에게 확인:
```
타겟: [#55] AddTriggerDialog — 유효하지 않은 Cron 표현식으로 트리거 생성 허용 (Major)
이대로 진행할까요?
```

사용자가 다른 이슈를 원하면 해당 이슈로 교체한다.

---

## Step 2. 이슈 분석

`docs/ISSUES.md`에서 해당 이슈 블록을 읽고 다음을 파악한다:

- **컴포넌트**: 수정 대상 파일 경로 및 라인 번호
- **현상**: 무엇이 잘못되었는가
- **원인**: 코드 레벨 근본 원인
- **수정 방향**: ISSUES.md에 명시된 권고 수정 방법

원인이 프론트엔드/백엔드 모두에 걸쳐 있는지 파악한다.

---

## Step 3. 계획 수립

`superpowers:writing-plans` 스킬을 호출하여 구현 계획을 작성한다.

계획에 반드시 포함할 항목:
- 수정할 파일 목록 (정확한 경로, 변경 위치)
- 각 파일에서 변경할 내용 (before → after)
- 프론트엔드 변경이 포함된 경우: E2E 테스트 추가 계획 (어떤 케이스를 검증할지)
- 테스트 실행 명령어

---

## Step 4. 구현

`superpowers:subagent-driven-development` 스킬을 호출하여 계획을 실행한다. 실행 방식은 항상 Subagent-Driven으로 진행하며 사용자에게 선택을 묻지 않는다.

**프론트엔드 변경 시 필수 사항**:
- `apps/firehub-web/CLAUDE.md`의 E2E 테스트 작성 원칙에 따라 회귀 테스트를 함께 작성한다
- 테스트는 "요소가 보이는가"가 아닌 입력→처리→출력 파이프라인을 검증해야 한다

**백엔드 변경 시 필수 사항**:
- TC 케이스가 있으면 해당 TC도 업데이트한다

---

## Step 5. 테스트 검증

구현 완료 후 빌드 및 테스트를 실행한다.

```bash
# 프론트엔드 변경 포함 시
cd apps/firehub-web && pnpm build && pnpm test:e2e

# 백엔드 변경 포함 시
cd apps/firehub-api && ./mvnw test -pl . -Dtest=<관련테스트클래스>

# 전체 빌드 확인
pnpm build
```

테스트 실패 시 원인을 파악하고 수정 후 재시도한다. 2회 이상 실패하면 사용자에게 상황을 보고하고 방향을 묻는다.

---

## Step 6. 재현 시나리오 검증 (이슈 해결 확인)

테스트 통과만으로는 충분하지 않다. ISSUES.md의 **재현** 단계를 그대로 따라가 버그가 실제로 사라졌는지 확인한다.

UI/프론트엔드 이슈는 `playwright-cli`로 실제 브라우저에서 검증한다:
- 서버가 실행 중이 아니면 `pnpm dev` 기동
- 재현 단계를 순서대로 실행
- 수정 전 발생하던 문제가 더 이상 발생하지 않는지 확인
- 수정 결과(정상 동작)를 스크린샷으로 저장: `test-results/issues/<이슈번호>/verified.png`

백엔드 API 이슈는 `curl`로 재현 시나리오를 재현한다.

**검증 실패 시**: Step 4로 돌아가 구현을 재검토한다.

---

## Step 7. ISSUES.md 업데이트

해당 이슈의 상태와 날짜를 업데이트한다:

```markdown
- **상태**: ✅ 수정 완료
- **수정**: YYYY-MM-DD
```

`🔴 미처리` → `✅ 수정 완료`로 변경하고, `**발견**:` 줄 바로 아래에 `**수정**:` 줄을 추가한다.

---

## Step 8. 커밋 (사용자 승인 후)

변경 내용을 요약하여 사용자에게 커밋 승인을 요청한다:

```
변경 파일:
- apps/firehub-web/src/pages/pipeline/components/AddTriggerDialog.tsx
- apps/firehub-web/e2e/pages/pipeline-trigger.spec.ts
- docs/ISSUES.md

커밋 메시지:
fix(pipeline): 유효하지 않은 Cron 표현식으로 트리거 생성 허용 (#55)

커밋할까요?
```

승인 시 커밋을 실행한다. 거부 시 사용자 지시를 따른다.

---

## 주의사항

- 이슈의 **수정 방향**은 참고 사항이며 코드를 실제 읽어 더 나은 방법이 있으면 적용해도 된다
- 수정 범위는 이슈에 명시된 컴포넌트에 집중하고, 관련 없는 코드는 건드리지 않는다
- E2E 테스트는 기존 `e2e/` 디렉토리 패턴을 따른다 (factories → fixtures → spec)
- 커밋 메시지: `fix(<도메인>): <이슈 제목 요약> (#이슈번호)` 형식
